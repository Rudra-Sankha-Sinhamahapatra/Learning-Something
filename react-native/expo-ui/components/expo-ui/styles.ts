import { StyleSheet } from "react-native";

export const commonStyles = StyleSheet.create({
  title: {
    fontSize: 16,
    fontWeight: "bold",
  },
  view: {
    flex: 1,
    gap: 10,
    marginBottom: 30,
  },
});
